const FooterItemService = {
  // Stubbed service: replace with real API calls
  getFooterItems: async () => {
    return { data: [] }
  },
  createFooterItem: async (payload) => {
    return { data: payload }
  },
  updateFooterItem: async (id, payload) => {
    return { data: payload }
  },
  deleteFooterItem: async (id) => {
    return { }
  }
}

export default FooterItemService
