const FooterCategoryService = {
  // Stubbed service: replace with real API calls
  getFooterCategories: async () => {
    return { data: [] }
  },
  createFooterCategory: async (payload) => {
    return { data: payload }
  },
  updateFooterCategory: async (id, payload) => {
    return { data: payload }
  },
  deleteFooterCategory: async (id) => {
    return { }
  }
}

export default FooterCategoryService
